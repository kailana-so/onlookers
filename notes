npm i bcrypt


npm i passport passport-local express-session express-flash 
npm dotenv
npm method-override

// Storing user data through pages => session library
// Displaying messages for if something was failed => flash library
// Storing configuration in the environment variables separate from the code => dotenv
// method override, overrides the post method with delete for logout delete route

